package collection;

public class A_collection {

	/*
	 * Collection contains of mechanism to store group of object together under one
	 * main The mechanism of a variable to store group of object together are- 
	 * 1. List 
	 * 2. Set 
	 * 3. Map 
	 * In collection, we can only store objects, if we try to add
	 * premitive type, it will be converted to a non-premitive type(object) and then
	 * store in the collection.
	 * Collection supports heterogeneous data(reference)
	 * Collections are growable in nature, the size of the collection can be increased 
	 * dynamically  at run time.
	 */

}
